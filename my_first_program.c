// #include <stdio.h>
#define MAX_STUDENT_COUNT 60

int main()
{
    int user_count = 0;
    // reading the user given student count
    puts("I love C programming");
    if (user_count <= MAX_STUDENT_COUNT)
        puts("The student count is less than or equal to 60");
}
